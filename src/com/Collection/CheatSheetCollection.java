package com.Collection;



/*
 * List                                               Queue                        Set                        Map
 * ArrayList                              		
 * Dynamic Array
 * 
 * Vector- same like array
 * Dynamic array(Thread safe)
 * 
 * LinkedList-Node Format
 * 
 * Null element
 * ArrayList-yes
 * LinkedList-yes
 * Vector-Yes
 * 
 * Order of element
 * ArrayList-Insertion order maintain
 * LinkedList-Insertion order maintain
 * Vector-Insertion order maintain
 * 
 * When to use
 *  ArrayList-Search operations are needed
 *  LinkedList-more modifications needed
 *  vector-Search operation but slow
 *  because it is thred safe
 * 
 * 
 * 
 * 
 * 
 * 
 *  Queue  
 *  Priority Queue
 *  ArrayDeque
 *  
 *   Priority Queue-uses resizable array to stoare object
 *   ArrayDeque-uses resizable array to stoare object
 *   
 *   
 *   null element
 *   Priority Queue-not accepts null value
 *   ArrayDeque-not accepts null value
 *   
 *   Duplicate
 *   Priority Queue-allowed
 *   ArrayDeque-allowed
 *   
 *   Order of element
 *   Priority Queue-insertion order maintained
 *   ArrayDeque-insertion order maintained
 *   
 *   when to use
 *   Priority Queue-it is used when you want a queue element placed in some specific order
 *   ArrayDeque-(both side entry exit)when you want to enter the element from both side
 *   
 *   
 *   
 *   Set
 *   HashSet
 *   LinkedHashSet
 *   TreeSet
 *   
 *   Internal Structure
 *   HashSet- internally uses a hashmap(Hash table-using array index)
 *   LinkedHashSet -internally uses LinkedHashMap(hash Table- Node)
 *   TreeSet- internally it uses TreeMap(Hash table)
 *   
 *   Null elements
 *   HashSet-accepts only 1 null element  because not accepts duplication
 *   LinkedHashSet-accepts only 1 null element  because not accepts duplication
 *   TreeSet-no null value
 *   
 *   Duplicate
 *    HashSet-no
 *   LinkedHashSet-no
 *   TreeSet-no
 *   
 *    HashSet-when we want unique element without order
 *   LinkedHashSet-when we want unique element with insertion order way
 *   TreeSet-when we want unique element without order
 *   
 *  
 *   Map
 *   HashMap
 *   LinkedHashMap
 *   TreeMap
 *   
 *   Internal Structure
 *   HashMap-uses hash table(stoared in random way)
 *   LinkedHashMap-hash table
 *   TreeMap-uses hash table(Stoared in sorted way)
 *   
 *   Null elements
 *   HashMap-Multiple null values are allowed but with no duplication of key
 *   TreeMap-Multiple null values are allowed but with no duplication of key
 *  
 *   Duplicate
 *   HashMap-duplication of values is allowed but not duplication of key
 *   TreeMap-duplication of values is allowed but not duplication of key
 *   
 *   Use-
 *   HashMap-when we want unique element without order
 *   TreeMap-when we want unique element with order
 *   
 *   
 *  
 *  
 *
 * 
 */
public class CheatSheetCollection {

	public static void main(String[] args) {
		
		
	}

}

